# Ground Link Extension (React)

## 安装依赖

```bash
npm install
```

## 开发

```bash
npm run dev
```

## 打包

```bash
npm run build
```

打包后的文件在 `dist/` 目录。

## 加载到 Chrome

1. 打开 `chrome://extensions/`
2. 开启"开发者模式"
3. 点击"加载已解压的扩展程序"
4. 选择 `dist/` 目录
